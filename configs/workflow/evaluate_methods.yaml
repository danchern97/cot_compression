# @package _global_

defaults:
  - /paths: default
  - /data: dolci_think_sft_600k
  - /method: qwen3/0_6b
  - /evaluation: answer_loss
  - /logging: wandb
  - _self_

mode: evaluate_methods

logging:
  project: qwen3-answer-loss
  log_file_name: eval.log
  tags:
    - qwen3
    - dolci
    - answer-loss

hydra:
  run:
    dir: ${paths.output_dir}/runs/eval/${data.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  job:
    chdir: true
