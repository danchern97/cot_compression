defaults:
  - paths: default
  - data: tiny_shakespeare_char
  - _self_

mode: prepare_data

hydra:
  run:
    dir: ${paths.output_dir}/runs/prepare_data/${now:%Y-%m-%d}/${now:%H-%M-%S}
  job:
    chdir: true
